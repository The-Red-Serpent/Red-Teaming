## Heap
The heap is a region of computer memory used for dynamic memory allocation, where data can be created and destroyed at runtime.Unlike the stack, memory in the heap does not automatically disappear when a function ends; it persists until the program explicitly frees it (or a garbage collector reclaims it).It is used to store objects, dynamically sized data structures, buffers, and any data that must outlive a single function call.

<p align="center">
   <img src="https://craftofcoding.wordpress.com/wp-content/uploads/2015/12/stackmemory4.jpg" alt="Architecture Diagram" width="600">
</p>

The heap lets us create memory dynamically when we don't know beforehand how much memory we need or how long we need it to live.

Heap memory is not automatically cleaned up when you are finished using it. In C, memory allocated with malloc() must be explicitly released using free(). If you don't release it, the memory can remain allocated and cause a memory leak. The heap lets us create memory dynamically when we don't know beforehand how much memory we need or how long we need it to live.

```
                    HEAP MEMORY
        ┌──────────────────────────────┐
        │                              │
        │   ┌──────────────┐           │
        │   │  Chunk A     │           │
        │   │  data = 10   │           │
        │   └──────────────┘           │
        │                              │
        │   ┌──────────────┐           │
        │   │  FREE SPACE   │          │
        │   └──────────────┘           │
        │                              │
        │   ┌──────────────┐           │
        │   │  Chunk B     │           │
        │   │  data = 20   │           │
        │   └──────────────┘           │
        │                              │
        │   ┌──────────────┐           │
        │   │  Chunk C     │           │
        │   │  data = 30   │           │
        │   └──────────────┘           │
        │                              │
        └──────────────────────────────┘


STACK / PROGRAM VARIABLES
        ┌──────────────┐
        │ p = address ─┼──────────────→ Chunk A
        ├──────────────┤
        │ q = address ─┼──────────────→ Chunk B
        ├──────────────┤
        │ r = address ─┼──────────────→ Chunk C
        └──────────────┘

```
